---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: discoverypolicies.puller.corewire.io
spec:
  group: puller.corewire.io
  names:
    kind: DiscoveryPolicy
    listKind: DiscoveryPolicyList
    plural: discoverypolicies
    singular: discoverypolicy
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.sources
      name: Sources
      priority: 1
      type: integer
    - jsonPath: .status.discoveredImages
      name: Images
      priority: 1
      type: integer
    - jsonPath: .status.lastSyncTime
      name: LastSync
      type: date
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DiscoveryPolicy is the Schema for the discoverypolicies API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DiscoveryPolicySpec defines the desired state of DiscoveryPolicy.
            properties:
              imageFilter:
                description: ImageFilter is a regex to filter discovered images.
                type: string
              maxImages:
                default: 50
                description: MaxImages caps the number of discovered images.
                format: int32
                minimum: 1
                type: integer
              sources:
                description: Sources is the list of discovery backends to query.
                items:
                  description: DiscoverySource defines a single discovery backend.
                  properties:
                    prometheus:
                      description: Prometheus config (when type=prometheus).
                      properties:
                        endpoint:
                          description: Endpoint is the Prometheus API URL.
                          minLength: 1
                          type: string
                        query:
                          description: Query is the PromQL query that must return
                            an 'image' label.
                          minLength: 1
                          type: string
                      required:
                      - endpoint
                      - query
                      type: object
                    registry:
                      description: Registry config (when type=registry).
                      properties:
                        imageTemplate:
                          description: |-
                            ImageTemplate is a Go text/template for constructing the full image reference.
                            Available variables: .Registry, .Repository, .Tag
                          type: string
                        repositories:
                          description: Repositories is the list of repositories to
                            query.
                          items:
                            type: string
                          minItems: 1
                          type: array
                        tagFilter:
                          description: TagFilter is a regex to filter tags.
                          type: string
                        topX:
                          description: TopX limits the number of tags to fetch per
                            repository.
                          format: int32
                          minimum: 1
                          type: integer
                        url:
                          description: URL is the registry base URL.
                          minLength: 1
                          type: string
                      required:
                      - repositories
                      - url
                      type: object
                    secretRef:
                      description: SecretRef references a Secret for auth/TLS for
                        this source.
                      properties:
                        name:
                          default: ""
                          description: |-
                            Name of the referent.
                            This field is effectively required, but due to backwards compatibility is
                            allowed to be empty. Instances of this type with an empty value here are
                            almost certainly wrong.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                    type:
                      description: Type identifies the backend.
                      enum:
                      - prometheus
                      - registry
                      type: string
                  required:
                  - type
                  type: object
                minItems: 1
                type: array
              syncInterval:
                default: 30m
                description: SyncInterval is how often to re-query sources.
                type: string
            required:
            - sources
            type: object
          status:
            description: DiscoveryPolicyStatus defines the observed state of DiscoveryPolicy.
            properties:
              conditions:
                description: Conditions represent the latest available observations.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              discoveredImages:
                description: DiscoveredImages is the list of discovered images from
                  all sources.
                items:
                  description: DiscoveredImage represents a single discovered image
                    with metadata.
                  properties:
                    image:
                      description: Image is the fully qualified image reference.
                      type: string
                    score:
                      description: Score is the ranking score from the source (higher
                        = more relevant).
                      format: int64
                      type: integer
                    source:
                      description: Source identifies which discovery source produced
                        this image.
                      type: string
                  required:
                  - image
                  - score
                  - source
                  type: object
                type: array
              lastSyncTime:
                description: LastSyncTime is the timestamp of the last successful
                  sync.
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
