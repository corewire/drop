{{- if .Values.metrics.secureServing }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "drop.fullname" . }}-metrics-reader
  labels:
    {{- include "drop.labels" . | nindent 4 }}
rules:
  - nonResourceURLs:
      - "/metrics"
    verbs:
      - get
{{- end }}
