# Discovery signal sample data

One representative day per image; the generator tiles it across a 48h window
(two identical days + a closing sample) to mirror the docs charts.

`weight` columns and selected windows are defined in the generator, not here,
because they describe the signal config, not the observed series.
