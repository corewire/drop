# Assert broken prometheus shows DNSError reason
apiVersion: drop.corewire.io/v1alpha1
kind: DiscoveryPolicy
metadata:
  name: test-broken-prom
status:
  (conditions[?type == 'Ready']):
    - status: "False"
      reason: DNSError
